#include <iostream>
#include <vector>
using namespace std;

int main() {
  vector<int> vect;
  int userInput = -99;

  vect.push_back(1);

  cout << "Size: " << vect.size() << "\n\n";
  
  while(userInput != -1) {
    int menuIndex;

    if(vect.size() > 0) {

    }
  }
  
  return 0;
}
