#include <iostream>
#include <string>
using namespace std;

int main() {
  // int x, y;

  // x = 20;

  // if(x)
  //   cout << "x";
  // if(y)
  //   cout << "y";

  // cout << endl;

  string str = "We think in generalities, but we live in details.";

  cout << str.substr(0, str.length()) << endl;
}
