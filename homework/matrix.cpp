#include <iostream>
using namespace std;

// template for the abstract data type 'MATRIX'
struct MATRIX {
  string name;
  double a, b, c, d; 
  // 2x2 matrix
  // [a b]
  // [c d]
};

int main() {
  
  
  return 0;
}
